# Top-level RUNTIME dependencies, floor pins. The ONLY file edited to bump versions (PLAN.md).
#   Windows ship lock:  pip-compile --generate-hashes   -> requirements-win.txt      (win_amd64, hashed)
#   WSL dev lock:        pip-compile requirements-dev.in -> requirements-dev.txt  (versions, no hashes)
# PySide6-Essentials, not the full PySide6 meta: the app imports only QtCore/QtGui/QtWidgets/
# QtNetwork (all in Essentials). The ~161 MB Addons set (QtWebEngine/Charts/Multimedia/QtPdf…) is
# unused — we render via PyMuPDF, not QtPdf — so excluding it shrinks the bundle + audit surface.
-r requirements-core.in   # PyMuPDF — the core's engine, shared with the bridge; pinned there (M115)
PySide6-Essentials>=6.7
# The floor is a SECURITY floor, moved 2026-09-05 (was >=6.15.0). pypdf 6.15.0 carries three
# crafted-PDF parse-DoS advisories — CVE-2026-84309 (infinite loop, fixed 6.16.0), CVE-2026-84310
# and CVE-2026-84311 (long runtime, both fixed 6.16.1) — so 6.16.1 is the first version clearing
# all three. Reached through `PyPdfEngine`'s `PdfReader`, which makes a crafted PDF the attack
# surface; the MCP bridge is unaffected, since requirements-mcp.txt ships no pypdf at all.
# Found by the `audit` job, not a person — the same way 6.14.2 -> 6.15.0 was (v0.17.1).
# NB this is a FLOOR: `pip-compile` resolves it to the newest release satisfying it, not to 6.16.1.
pypdf>=6.16.1
